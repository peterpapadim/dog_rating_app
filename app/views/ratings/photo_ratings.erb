<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" type="text/css" href="/style.css">

    <title>Photo</title>
  </head>
  <body>
    <a href="/" id="er">Home</a><a href="/users" id="er">All Puppy Lovers</a><br>
    <h2><%= @photo.title %></h2>
    <a href="/users/<%= @photo.user.id %>">Back to <%= @photo.user.name %>'s photos</a><br>
    <img src="<%= @photo.url %>" alt="adorable puppy"><br>
    <h3>Average rating: <%= @photo.avg_rating %></h3><br>
    <ul>

    <% @photo.ratings.each do |rating| %>
    <li><%= rating.user.name %> rated this photo <%= rating.score %> / 10</li>
    <% end %>
  </ul>
  <br>


  </body>
</html>
