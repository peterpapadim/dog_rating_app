<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Photo</title>
  </head>
  <body>
    <h2><%= @photo.title %></h2>
    <img src="<%= @photo.url %>" alt="adorable puppy"><br>
    <h3>Average rating: <%= @photo.avg_rating %></h3><br>
    <% if @photo.ratings.count > 0 %>
    <a href="/photos/<%= @photo.id %>/ratings">View all ratings</a>
    <% end %>
    <a href="/photos/<%= @photo.id %>/edit">Edit</a>
    <a href="/photos/<%= @photo.id %>/ratings/new">Rate</a>

  </body>
</html>
